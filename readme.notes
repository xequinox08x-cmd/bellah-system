MONTH 1 — Foundation + Inventory
─────────────────────────────────────────────────────────

Week 1: Project Setup                                ✅ DONE
├── Monorepo (apps/web + apps/api, pnpm workspaces)
├── Express + TypeScript API
├── Neon DB connection + pool.ts
└── GET /health returns { ok, db, time }

Week 2: Products API                                 ✅ DONE
├── Products table in schema.sql
├── GET /api/products
├── POST /api/products
├── PUT /api/products/:id
└── DELETE /api/products/:id

Week 3: Inventory UI → API                           ✅ DONE
├── Products page wired to real API
├── Add / Edit / Delete persists in DB
├── Removed mock store for products
└── Price/cost display with .toFixed(2)

Week 4: Validation + UX                              ✅ DONE
├── Duplicate SKU rejected (400)
├── Non-negative price/cost/stock enforced
├── Loading states + Sonner toasts
└── Friendly error messages


MONTH 2 — Sales + Dashboard
─────────────────────────────────────────────────────────

Week 5: Sales Schema + Transaction                   ✅ DONE
├── sales + sale_items tables in schema.sql
├── POST /api/sales (transaction: insert + deduct stock)
├── Reject sale if any item would go negative
└── GET /api/sales, GET /api/sales/:id

Week 6: Sales UI                                     ✅ DONE
├── Sales page wired to real API
├── Record sale form (select product + qty)
├── Sales history list
└── Toast on success / error on insufficient stock

Week 7: Dashboard Metrics                            ✅ DONE
├── GET /api/dashboard/summary
├── Sales today / week / month from DB
├── Top products by quantity sold
├── Low stock alerts
├── AdminDashboard + StaffDashboard show real data
└── Loading + error states

Week 8: Dashboard Polish                             ✅ DONE
├── Fetch logic moved inside components
├── Pagination on list endpoints
└── Design shell merged (Sidebar, TopNav, Login)


MONTH 3 — Marketing + AI + Campaigns
─────────────────────────────────────────────────────────

Week 9: AI Content API                               ← YOU
├── ai_content table migration
├── POST /api/content (save draft)
├── GET /api/content (list + ?status= filter)
└── PATCH /api/content/:id/status (approve/reject)

Week 10: AI Content UI                               ← YOU
├── Content list page with status filters
├── Create draft form
├── Approve / Reject actions (admin only)
└── POST /api/ai/generate stub

Week 11: Campaigns API                               ← TEAMMATE
├── campaigns + campaign_content tables migration
├── GET/POST /api/campaigns
├── PUT/DELETE /api/campaigns/:id
└── POST /api/campaigns/:id/content (attach approved only)

Week 12: Campaigns + Scheduling UI                   ← TEAMMATE
├── scheduled_posts table migration
├── GET/POST /api/scheduled-posts
├── Campaigns page (create, attach content)
└── Scheduling page (list posts with status)


MONTH 4 — Auth + Deployment
─────────────────────────────────────────────────────────

Week 13: Clerk Auth                                  ← SPLIT
├── YOU:      ClerkProvider in apps/web
│             Replace mock AuthContext with useUser()
│             Protect routes, hide admin UI for staff
│
└── TEAMMATE: @clerk/express middleware in apps/api
              requireAuth on all protected routes
              requireAdmin on admin-only routes
              Sync clerk_id + role into users table

Week 14: Deployment                                  ← SPLIT
├── YOU:      Deploy apps/web to Vercel
│             Set VITE_API_URL env var
│             Confirm lazy-loaded routes work in prod
│
└── TEAMMATE: Deploy apps/api to Render / Fly.io
              Set DATABASE_URL, CLERK_SECRET_KEY, CORS_ORIGIN
              Run all migrations on Neon production branch
              Confirm /health returns { ok: true }

Week 15: Demo Prep                                   ← BOTH
├── End-to-end smoke test
│   Add product → record sale → stock deducts →
│   dashboard updates → create content → approve →
│   attach to campaign → schedule
├── Fix any bugs found in smoke test
├── Seed realistic demo data
└── Final walkthrough with advisor
